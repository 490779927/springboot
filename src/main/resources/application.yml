server:
  port: 8080
mybatis-plugs:
  mapper-locations: classpath:mappings/generate/*.xml
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/crk5?serverTimezone=UTC&useSSL=false&autoReconnect=true&tinyInt1isBit=false&useUnicode=true&characterEncoding=utf8
    username: yang
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    database: 0
    host: localhost
    port: 6379
    password:
  rabbitmq:
    host: localhost
    port: 5672
    username: yang
    password: 123456
logging:
  level:
    io:
      swagger:
        models:
          parameters:
            AbstractSerializableParameter: error